export default function SectionLabel({ children }) {
  return (
    <span
      className="block font-mono text-[10px] uppercase tracking-[0.12em] text-text-tertiary mb-3"
    >
      {children}
    </span>
  );
}
